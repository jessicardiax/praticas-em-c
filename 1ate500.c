/*/Escreva um programa que exibe o somatório dos valores pares existentes na faixa
de 1 (inclusive) até 500 (inclusive)./*/

#include <stdio.h>
int main (){
    int cont=1;
    printf("numeros pares de 1 ate 500:");
    for (cont =1 ; cont <=500 ; cont++){
        printf(" %i " ,cont);
    }
return(0);        
    }
