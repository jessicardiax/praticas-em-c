#include <stdio.h>
    int main(){
        float r,a;
        printf("informe o raio:");
        scanf("%f",&r);
        a = r*2;
        printf("a area da circunferencia é: %f\n",a);
        return (0);
    }
